testPort: 9091
server:
  applicationConnectors:
  - type: http
  adminConnectors:
  - type: http
logging:
  level: INFO
  loggers:
    org.metadatacenter: DEBUG
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
